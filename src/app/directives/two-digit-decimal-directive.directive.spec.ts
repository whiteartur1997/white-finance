import { TwoDigitDecimalDirectiveDirective } from './two-digit-decimal-directive.directive';

describe('TwoDigitDecimalDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TwoDigitDecimalDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
